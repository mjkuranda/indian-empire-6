/* Village data */
var village = {
    id:     -1, // province id
    name:   '', // province name
    owner:  -1, // user id
    init: function (id, owner) { // set the variables
        this.id     = id;
        this.owner  = owner;
    }
};